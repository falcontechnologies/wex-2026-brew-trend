# =============================================================================
# requirements.txt — Homebrew API Tracker
# =============================================================================
# Install all required dependencies:
#   pip install -r requirements.txt
#
# Install including optional dependencies:
#   pip install -r requirements.txt schedule openpyxl
#
# Note: sqlite3 is part of the Python standard library and does not need to
#       be installed separately.
# =============================================================================


# -----------------------------------------------------------------------------
# Required Dependencies
# -----------------------------------------------------------------------------

requests==2.32.3
    # HTTP library for making API calls to the Homebrew Formulae JSON API


# -----------------------------------------------------------------------------
# Optional Dependencies
# -----------------------------------------------------------------------------
# Uncomment the packages below as needed, or install them individually.

# schedule==1.2.2
    # Pure-Python job scheduler for running data collection at regular intervals
    # Used by: setup_scheduler.py

# openpyxl==3.1.5
    # Read/write Excel (.xlsx) files for exporting analytics data
    # Used by any future Excel export functionality
